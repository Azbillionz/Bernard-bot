Build a flawless, enterprise-grade, multi-chain Telegram trading and sniping bot named QUANTREXTRADING_BOT using Node.js, TypeScript, and Telegraf, explicitly designed to scale globally using an Express.js server for Telegram webhooks, Redis with BullMQ for background worker queues to strictly throttle outbound messages under 30 per second, and Prisma ORM with PostgreSQL connection pooling to eliminate database locks. The entire codebase must be strictly typed, utilizing optional chaining (?.) and nullish coalescing (??) across every object access to guarantee a completely 'undefined-proof' architecture, wrapped in global exception boundaries (process.on('uncaughtException') and process.on('unhandledRejection')) and auto-healing WebSocket connections with exponential-backoff reconnect strategies. You must absolutely eliminate any 'coming soon...' or placeholder strings, replacing 100% of the inline keyboard routing logic with functional, production-ready backend implementations. The main dashboard menu must render via ctx.editMessageText to prevent chat spam, formatting dynamically as: '⚡ SILENT SNIPER | 🌐 Active Chain: {SOL/ETH/BASE/BSC}\n—\n💼 Wallet: {pubkey_or_tap_connect}\n💰 Balance: {live_balance} {chain_symbol}\n🤖 Auto-Snipe: {🔴 OFF / 🟢 ON}\n📈 Open Trades: {count_from_db}\n📡 Scanner: {🟢 Active / 🔴 Inactive}\n🎯 Signals Sent: {count_from_db}\n💲 Native Token Price: ${live_fetched_price}\n—\nSend any CA ({Solana base58} or {EVM 0x...}) to analyze it instantly.\nUse the menu below to scan, snipe, and track trades.' The inline keyboard matrix must map out exactly 14 functional buttons across 7 rows: Row 1 handles [🔍 New Runners] and [🔥 Trending] which programmatically query DexScreener's real-time boost and volume endpoints to return the top 5 trending pairs; Row 2 maps [🌱 PumpFun / Moonshot Snipe] connecting to WSS log listeners to monitor token creations and [📋 Previous Signals] fetching historical triggers from the database; Row 3 handles [💼 Wallet Manager] which queries live RPC balances, displays multi-chain keys encrypted via Node's crypto module (AES-256-GCM), and enables key generation or base58 importing, alongside [🔄 Copy-Trade] which records and tracks target wallets inside a custom database schema; Row 4 contains [📡 Group Scanner] to parse inbound messages for contract addresses and [📊 PnL Center] alongside Row 5's [📉 My Trades] to run a prisma.trade.findMany() query calculating true realized and unrealized PnL; Row 5 also holds [🤖 Auto-Snipe {🔴/🟢}] to instantly toggle user database booleans and reload the dashboard UI. Row 6 maps [⚙️ Settings] for switching active chains and adjusting configurations, and [⚗️ Filters] to enforce automated safety thresholds (minimum liquidity, honeypot safety, and max tax limits) before any auto-buy executes. Row 7 maps [📊 Bot Stats] and [❓ Help & Guide]. The bot must listen for contract addresses via bot.on('text'), separating 42-character EVM addresses from 43-44 character Solana addresses, querying api.dexscreener.com to return rich token stats (Name, Ticker, CA, Price USD, 5m/1h/24h changes, Market Cap, Liquidity USD) coupled with localized security checks (GoPlus API for EVM honeypot/tax scanning, and mint/freeze authority checks for Solana), displaying inline trading controls like [Buy 0.1], [Buy 0.5], [Buy Custom], [Sell 50%], and [Sell 100%]. For real-world execution, the bot must hardcode a non-custodial 1% platform fee (100 basis points) on every transaction, utilizing Jupiter V6 API's platformFeeBps parameters routed to DEV_FEE_WALLET on Solana, and the equivalent integrator fee arguments on 1inch/0x APIs for EVM chains. To protect transactions globally, all swaps must undergo local pre-execution simulations (eth_call for EVM and simulateTransaction for Solana) and route transactions completely outside the public mempools using the Jito Block Engine with dynamic tips for Solana bundles, and the Flashbots Private RPC for EVM chains to eliminate front-running or sandwich attacks. Generate a complete project structure including package.json, tsconfig.json, schema.prisma tracking users, wallets, trades, and sniper configurations, and a comprehensive .env.example file populated with variables for TELEGRAM_BOT_TOKEN, DATABASE_URL, ENCRYPTION_SECRET, SOLANA_RPC_URL, SOLANA_WSS_URL, JITO_BLOCK_ENGINE_URL, DEFAULT_SOL_JITO_TIP, ETH_RPC_URL, FLASHBOTS_RPC_URL, BASE_RPC_URL, BSC_RPC_URL, DEXSCREENER_API_URL, GOPLUS_SECURITY_API, REDIS_URL, and DEV_FEE_WALLET.